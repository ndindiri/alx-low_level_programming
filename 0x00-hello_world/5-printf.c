#include <stdio.h>
/**
 * main -this is the  main function
 * Return: retun 0 if instructions are successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
